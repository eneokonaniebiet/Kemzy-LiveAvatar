name: Kémzy Android Live Driver

on:
  push:
    branches: ["feature/backend-render-gateway"]
  pull_request:
    branches: ["feature/backend-render-gateway"]

jobs:
  backend:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - run: pip install -r backend/requirements.txt pytest
      - run: PYTHONPATH=. pytest -q backend/tests
      - run: python -m py_compile backend/zerogpu_space/app.py backend/zerogpu_space/liveportrait_adapter.py backend/zerogpu_space/motion_contract.py backend/renderer/personalive_server.py
      - run: sh -n backend/renderer/start_personalive.sh
      - run: grep -q 'Pipeline.InputParams' backend/renderer/personalive_server.py
      - run: grep -q 'GPU_REQUIRED' backend/renderer/personalive_server.py

  android:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: android
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "17"
      - uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: "8.10.2"
      - run: gradle testDebugUnitTest assembleDebug --no-daemon
      - uses: actions/upload-artifact@v4
        with:
          name: kemzy-liveavatar-debug-apk
          path: android/app/build/outputs/apk/debug/app-debug.apk
